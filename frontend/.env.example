# Base URL of the deployed backend API (include the trailing /api). Only needed for production
# builds - local dev falls back to http://localhost:4000/api automatically.
VITE_API_BASE_URL=https://your-backend.up.railway.app/api
